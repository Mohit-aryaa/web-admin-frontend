import { CameltostringPipe } from './cameltostring.pipe';

describe('CameltostringPipe', () => {
  it('create an instance', () => {
    const pipe = new CameltostringPipe();
    expect(pipe).toBeTruthy();
  });
});
