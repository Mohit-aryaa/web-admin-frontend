import { GetCategoryNameByIdPipe } from './get-category-name-by-id.pipe';

describe('GetCategoryNameByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCategoryNameByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
