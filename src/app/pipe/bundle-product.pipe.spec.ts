import { BundleProductPipe } from './bundle-product.pipe';

describe('BundleProductPipe', () => {
  it('create an instance', () => {
    const pipe = new BundleProductPipe();
    expect(pipe).toBeTruthy();
  });
});
