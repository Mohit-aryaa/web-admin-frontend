import { GetSubCategoryNameByIdPipe } from './get-sub-category-name-by-id.pipe';

describe('GetSubCategoryNameByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSubCategoryNameByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
